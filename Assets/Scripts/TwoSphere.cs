using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TwoSphere : MonoBehaviour
{
    public string Name => nameof(TwoSphere);

    // Update is called once per frame
    void Update()
    {
        
    }
}
