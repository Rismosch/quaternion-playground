using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ThreeSphere : MonoBehaviour
{
    public string Name => nameof(ThreeSphere);

    // Update is called once per frame
    void Update()
    {
        
    }
}
